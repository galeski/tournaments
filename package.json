{
  "name": "tournaments",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "node index.js",
    "server": "npm start --prefix server/",
    "client": "npm run dev --prefix client/",
    "dev": "concurrently \"npm run --prefix server/\" \"npm run --prefix client/\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.4"
  }
}
